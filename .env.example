# Cadena de conexión de Neon (Postgres). Cópiala como .env.local y no la subas al repo.
DATABASE_URL="postgresql://usuario:password@host/neondb?sslmode=require"

# Cuando se conecten las APIs de tarifas reales:
# IGNAV_API_KEY=
# FLIGHTAPI_KEY=
